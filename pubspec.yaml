name: device_playground
description: "A playground for comparing device features on real hardware"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: 3.4.2

dev_dependencies:
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true
