name: ios_photo_kit_gateway
description: "iOS PhotoKit collection operations"
publish_to: "none"

version: 0.1.0+1
environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 5.0.0

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: IosPhotoKitGatewayPlugin
